"use strict"

function abc(a)
{

 		
	if (a <= 18){
	a = "доступ закрыт";
	return (a);} else 
			{ 	
				a ="Доступ открыт"; 
					return (a);
			}
}

let dostup;
dostup = abc(19);
//dostup =12;
alert(dostup);